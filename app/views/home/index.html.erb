<section class="py-5 text-center container">
	<div class="row py-lg-5">
	  <div class="col-lg-6 col-md-8 mx-auto">
	    <h1 class="fw-light">Welcome to Your Work Tracker</h1>
	    <p class="lead text-body-secondary">We track all of your assignments giving you the flexibilty to categorize each one!</p>
	    <p>
	      <a href="<%= to_dos_url %>" class="btn btn-secondary my-2"><i class="bi bi-journals"></i> See Your To Dos</a>

	      <!-- 
	      <% if !logged_in? %>
	      	<a href="<%= login_path %>" class="btn btn-success my-2"><i class="bi bi-pencil-square"></i> Sign In</a>
	      	<a href="<%= new_user_path %>" class="btn btn-success my-2"><i class="bi bi-pencil-square"></i> Sign Up</a>
	      <% else %>
	      	<a href="<%= user_path current_user %>" class="btn btn-success my-2"><i class="bi bi-pencil-square"></i> <%= current_user.name %></a>
	      	
	      	<%= button_to "<i class='bi bi-trash'></i> Log Out".html_safe, {:controller => :sessions, :action => 'destroy'}, method: :delete, class: "btn btn-danger" %>

	      <% end %>
	      -->
	      
	    </p>
	  </div>
	</div>
</section>
